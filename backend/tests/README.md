# tests
Vitest test suites (unit + integration), mirroring src/ structure. Real coverage starts Phase 12, though tests should be added incrementally per phase where practical rather than deferred wholesale.

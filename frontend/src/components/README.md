# components
auth/, chat/, tickets/, knowledge/, agent/, admin/ — feature-scoped components, populated as each phase builds that area.
common/ — shared/reusable components (buttons, inputs, layout primitives), populated starting Phase 5.
